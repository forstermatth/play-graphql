'use strict';
import assert from 'assert';
import playGraphql from '../lib';

describe('play-graphql', function () {
  it('should have unit test!', function () {
    assert(false, 'we expected this package author to add actual unit tests. damn.');
  });
});
